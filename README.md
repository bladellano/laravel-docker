# Laravel-docker
## Up and running
```
$ git clone git@github.com:bladellano/laravel-docker.git
$ cd laravel-docker 
$ cd laravel-app 
$ cp .env.example .env
$ cd ..
$ make setup
$ make perm-storage
```